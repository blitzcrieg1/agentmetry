---
description: AgentAudit vault and docs
globs: vault/**,docs/**
alwaysApply: false
---

# Vault + docs

## Shipped skill

- `audit_demo` — pipeline graph, one governed tool call, human approval gate

## Vault layout

`00-Inbox/` · `20-Active-Loops/` (runtime) · `30-Archive/` (runtime) · `.system/skill-definitions/`

Driver config: copy `drivers.json.example` → `drivers.json` (gitignored).

Use `Set-Content -Encoding utf8` on Windows — not `echo >` (UTF-16 breaks file reads).

## Key docs

- `docs/agentaudit-dogfood-checklist.md` — validation
- `docs/external-agent-audit.md` — Tier B IDE hooks
- `docs/agent-audit-event-schema.md` — canonical JSON v1.1
