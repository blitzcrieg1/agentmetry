id: inbox-document-summarize
type: vault_watch
enabled: true
skill: doc_summarize
cooldown_seconds: 300
match:
  path_glob: "00-Inbox/*"
  extensions:
    - .pdf
    - .docx
# doc_summarize docs.read_document expects the bare vault-relative path.
user_input_template: "{{ trigger.file_path }}"
