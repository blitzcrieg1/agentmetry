"""Driver subsystem — MCP tool servers mounted from vault/.system/drivers.json.

MCP replaces the tool layer, not graph orchestration: skills stay checkpointed
LangGraph pipelines; drivers give their nodes governed capabilities.
"""
