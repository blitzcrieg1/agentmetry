{
  "name": "agentmetry-dashboard",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "dependencies": {
    "@base-ui/react": "^1.7.0",
    "@xyflow/react": "^12.11.6",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "lucide-react": "^0.577.0",
    "next": "16.3.4",
    "next-themes": "^0.4.6",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "recharts": "^3.10.1",
    "shadcn": "^4.20.1",
    "tailwind-merge": "^2.6.0",
    "tw-animate-css": "^1.4.0",
    "zustand": "^5.0.15"
  },
  "devDependencies": {
    "@types/node": "^26",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.7",
    "@vitejs/plugin-react": "^6.1.1",
    "autoprefixer": "^10.5.4",
    "eslint": "^9.39.5",
    "eslint-config-next": "16.3.4",
    "jsdom": "^25.0.1",
    "postcss": "^8.5.28",
    "tailwindcss": "^3.4.17",
    "typescript": "^5",
    "vitest": "^5.0.0"
  },
  "overrides": {
    "sharp": "^0.35.4",
    "@hono/node-server": "^1.19.17",
    "fast-uri": "^3.1.7"
  },
  "overridesNote": {
    "reason": "sharp is an optional transitive of next. Its only caller is the image optimizer, which next.config.js disables (images.unoptimized) and which cannot run in a static export anyway, so the libvips advisory is unreachable here. The override originally closed that alert without taking Next 16, which is what Dependabot's sharp PR actually proposed at the time. Next 16 has since been taken and now declares sharp ^0.35.4 as its own optional dependency, so this entry is redundant and resolves to the same 0.35.4 with or without it. It is safe to delete in a change that regenerates the lockfile.",
    "remove_when": "Met on 2026-09-12: next 16.3.4 depends on sharp ^0.35.4 itself. The entry is kept only until a change that regenerates package-lock.json can drop it.",
    "transitive_advisories": "@hono/node-server, brace-expansion and fast-uri are pinned forward here because each is a transitive dependency whose parent has not cut a release. Dependabot opened one PR per package and each invalidated the others' lockfile on merge, so they are done once, together. Remove an entry when its parent ships a version that satisfies the advisory on its own.",
    "postcss_not_overridden": "Resolved. This used to say next 15.5.21 pinned postcss to exactly 8.4.31 and bundled it, so the advisory stayed open rather than force a version past a framework's exact pin. The Next 16 migration named in remove_when has since happened: next is 16.3.4 and postcss resolves to 8.5.28 on its own. No override was ever needed and none is used. Kept as a record because the original note outlived the fact it described and would have told the next reader to leave open something already closed."
  }
}
