title: Agentmetry - Local content piped into an interpreter
id: 7ce5d9e8-bf17-513d-b06d-f33bf78e77ff
status: experimental
description: |
  Agentmetry sequence detection `encoded-command-download` fired. The individual tool calls are
  ordinary; the order is the finding, and `detection.event_ids` names the exact
  events that produced it.
  This rule id emits more than one severity. This is the `low` variant, pinned on `action.outcome`.

  GENERATED by tools/generate_sigma_pack.py. Do not hand-edit: severity and
  MITRE ids are read from the rule engine, so an edit here is a claim that
  disagrees with the code.
references:
  - https://github.com/blitzcrieg1/agentmetry/blob/master/docs/detection-rules.md
author: Agentmetry
logsource:
  product: agentmetry
  service: audit
detection:
  selection:
    action.type: detection
    detection.rule_id: encoded-command-download
    action.outcome: low
  condition: selection
falsepositives:
  - Legitimate automation that reproduces the sequence. Disposition it in the
    trail rather than deleting the rule, so the decision is recorded.
level: low
tags:
  - attack.ta0002
  - attack.t1059
